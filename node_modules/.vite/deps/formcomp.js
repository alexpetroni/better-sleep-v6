import {
  add_locations,
  check_target,
  clsx,
  each,
  hmr,
  if_block,
  key,
  legacy_api,
  prop,
  remove_input_defaults,
  set_attribute,
  set_checked,
  set_class,
  set_style,
  set_value,
  snippet,
  wrap_snippet
} from "./chunk-XZDNTRGL.js";
import "./chunk-U7P2NEEE.js";
import {
  append,
  comment,
  from_html,
  set_text
} from "./chunk-VWWCNVDR.js";
import {
  apply,
  delegate,
  delegated
} from "./chunk-EGTEDAW4.js";
import {
  FILENAME,
  HMR,
  add_svelte_meta,
  child,
  first_child,
  get2 as get,
  getContext,
  next,
  pop,
  proxy,
  push,
  remove_textarea_child,
  reset,
  set,
  setContext,
  sibling,
  state,
  strict_equals,
  tag,
  tag_proxy,
  template_effect,
  update,
  user_derived
} from "./chunk-W6TT6Y22.js";
import "./chunk-XWATFG4W.js";
import "./chunk-HNWPC2PS.js";
import "./chunk-GLFJSY72.js";
import "./chunk-OHYQYV5R.js";

// node_modules/formcomp/dist/types.js
var FORM_STATE_KEY = /* @__PURE__ */ Symbol("form-state");
var TRANSLATE_KEY = /* @__PURE__ */ Symbol("form-translate");
var STEP_ID_KEY = /* @__PURE__ */ Symbol("form-step-id");

// node_modules/formcomp/dist/state/form-state.svelte.js
function createFormState(config, options = {}) {
  const {
    persist = "sessionStorage",
    storageKey = "formcomp-state",
    debounceMs = 300
  } = options;
  const initial = {};
  for (const step of config.steps) {
    initial[step.id] = {};
  }
  let hydrated = initial;
  let hydratedIndex = 0;
  if (persist && strict_equals(typeof window, "undefined", false)) {
    try {
      const storage = strict_equals(persist, "sessionStorage") ? sessionStorage : localStorage;
      const stored = storage.getItem(storageKey);
      if (stored) {
        const parsed = JSON.parse(stored);
        if (parsed.responses) hydrated = { ...initial, ...parsed.responses };
        if (strict_equals(typeof parsed.currentStepIndex, "number")) hydratedIndex = parsed.currentStepIndex;
      }
    } catch {
    }
  }
  let responses = tag_proxy(proxy(hydrated), "responses");
  let currentStepIndex = tag(state(proxy(hydratedIndex)), "currentStepIndex");
  let saveTimer;
  function scheduleSave() {
    if (!persist || strict_equals(typeof window, "undefined")) return;
    clearTimeout(saveTimer);
    saveTimer = setTimeout(
      () => {
        try {
          const storage = strict_equals(persist, "sessionStorage") ? sessionStorage : localStorage;
          storage.setItem(storageKey, JSON.stringify({ responses, currentStepIndex: get(currentStepIndex) }));
        } catch {
        }
      },
      debounceMs
    );
  }
  return {
    get currentStepIndex() {
      return get(currentStepIndex);
    },
    set currentStepIndex(v) {
      set(currentStepIndex, v, true);
      scheduleSave();
    },
    get currentStepId() {
      return config.steps[get(currentStepIndex)]?.id ?? "";
    },
    get stepCount() {
      return config.steps.length;
    },
    nextStep() {
      if (get(currentStepIndex) < config.steps.length - 1) {
        update(currentStepIndex);
        scheduleSave();
      }
    },
    prevStep() {
      if (get(currentStepIndex) > 0) {
        update(currentStepIndex, -1);
        scheduleSave();
      }
    },
    goToStep(index) {
      if (index >= 0 && index < config.steps.length) {
        set(currentStepIndex, index, true);
        scheduleSave();
      }
    },
    getResponse(stepId, questionId) {
      return responses[stepId]?.[questionId];
    },
    setResponse(stepId, questionId, value) {
      if (!responses[stepId]) {
        responses[stepId] = {};
      }
      responses[stepId][questionId] = value;
      scheduleSave();
    },
    getStepResponses(stepId) {
      return responses[stepId] ?? {};
    },
    get allResponses() {
      return responses;
    }
  };
}

// node_modules/formcomp/dist/conditions/evaluator.js
function isCompound(c) {
  return "conditions" in c;
}
function evaluateSimple(condition, getResponse, currentStepId) {
  const stepId = condition.stepId ?? currentStepId;
  const response = getResponse(stepId, condition.questionId);
  switch (condition.operator) {
    case "equals":
      return response === condition.value;
    case "not-equals":
      return response !== condition.value;
    case "includes": {
      if (Array.isArray(response)) {
        return response.includes(condition.value);
      }
      return false;
    }
    case "not-includes": {
      if (Array.isArray(response)) {
        return !response.includes(condition.value);
      }
      return true;
    }
  }
}
function evaluateCondition(condition, getResponse, currentStepId) {
  if (isCompound(condition)) {
    const results = condition.conditions.map((c) => evaluateCondition(c, getResponse, currentStepId));
    return condition.operator === "and" ? results.every(Boolean) : results.some(Boolean);
  }
  return evaluateSimple(condition, getResponse, currentStepId);
}

// node_modules/formcomp/dist/validation/validator.js
function isFilled(value) {
  if (value === void 0 || value === null)
    return false;
  if (typeof value === "string")
    return value.trim().length > 0;
  if (typeof value === "number")
    return true;
  if (Array.isArray(value))
    return value.length > 0;
  return true;
}
function isQuestionVisible(question, getResponse, currentStepId) {
  if (!question.condition)
    return true;
  return evaluateCondition(question.condition, getResponse, currentStepId);
}
function validateStep(stepConfig, getResponse, currentStepId) {
  for (const group of stepConfig.groups) {
    if (group.condition) {
      const visible = evaluateCondition(group.condition, getResponse, currentStepId);
      if (!visible)
        continue;
    }
    for (const question of group.questions) {
      if (!question.required)
        continue;
      if (!isQuestionVisible(question, getResponse, currentStepId))
        continue;
      const value = getResponse(currentStepId, question.id);
      if (!isFilled(value)) {
        return { isValid: false, firstIncompleteGroupId: group.id };
      }
    }
  }
  return { isValid: true, firstIncompleteGroupId: null };
}

// node_modules/formcomp/dist/components/layout/ProgressBar.svelte
ProgressBar[FILENAME] = "node_modules/formcomp/dist/components/layout/ProgressBar.svelte";
var root_2 = add_locations(from_html(`<button type="button" class="group flex w-full items-center"><span class="flex items-center px-6 py-4 text-sm font-medium"><span class="flex size-10 shrink-0 items-center justify-center rounded-full bg-indigo-600 group-hover:bg-indigo-800"><svg class="size-6 text-white" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M19.916 4.626a.75.75 0 0 1 .208 1.04l-9 13.5a.75.75 0 0 1-1.154.114l-6-6a.75.75 0 0 1 1.06-1.06l5.353 5.353 8.493-12.74a.75.75 0 0 1 1.04-.207Z" clip-rule="evenodd"></path></svg></span> <span class="ml-4 text-sm font-medium text-gray-900"> </span></span></button>`), ProgressBar[FILENAME], [[25, 5, [[30, 6, [[31, 7, [[32, 8, [[33, 9]]]]], [36, 7]]]]]]);
var root_3 = add_locations(from_html(`<span class="flex items-center px-6 py-4 text-sm font-medium" aria-current="step"><span class="flex size-10 shrink-0 items-center justify-center rounded-full border-2 border-indigo-600"><span class="text-indigo-600"> </span></span> <span class="ml-4 text-sm font-medium text-indigo-600"> </span></span>`), ProgressBar[FILENAME], [[40, 5, [[41, 6, [[42, 7]]], [44, 6]]]]);
var root_4 = add_locations(from_html(`<span class="group flex items-center"><span class="flex items-center px-6 py-4 text-sm font-medium"><span class="flex size-10 shrink-0 items-center justify-center rounded-full border-2 border-gray-300"><span class="text-gray-500"> </span></span> <span class="ml-4 text-sm font-medium text-gray-500"> </span></span></span>`), ProgressBar[FILENAME], [[47, 5, [[48, 6, [[49, 7, [[50, 8]]], [52, 7]]]]]]);
var root_5 = add_locations(from_html(`<div class="absolute top-0 right-0 hidden h-full w-5 md:block" aria-hidden="true"><svg class="size-full text-gray-300" viewBox="0 0 22 80" fill="none" preserveAspectRatio="none"><path d="M0 -2L20 40L0 82" vector-effect="non-scaling-stroke" stroke="currentcolor" stroke-linejoin="round"></path></svg></div>`), ProgressBar[FILENAME], [[59, 5, [[60, 6, [[61, 7]]]]]]);
var root_1 = add_locations(from_html(`<li class="relative md:flex md:flex-1"><!> <!></li>`), ProgressBar[FILENAME], [[23, 3]]);
var root = add_locations(from_html(`<nav aria-label="Progress" class="mb-8"><ol role="list" class="divide-y divide-gray-300 rounded-md border border-gray-300 md:flex md:divide-y-0"></ol></nav>`), ProgressBar[FILENAME], [[17, 0, [[18, 1]]]]);
function ProgressBar($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ProgressBar);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  var $$exports = { ...legacy_api() };
  var nav = root();
  var ol = child(nav);
  add_svelte_meta(
    () => each(ol, 23, () => $$props.steps, (step) => step.id, ($$anchor2, step, i) => {
      const isCompleted = tag(user_derived(() => get(i) < $$props.currentIndex), "isCompleted");
      get(isCompleted);
      const isCurrent = tag(user_derived(() => strict_equals(get(i), $$props.currentIndex)), "isCurrent");
      get(isCurrent);
      const isUpcoming = tag(user_derived(() => get(i) > $$props.currentIndex), "isUpcoming");
      get(isUpcoming);
      var li = root_1();
      var node = child(li);
      {
        var consequent = ($$anchor3) => {
          var button = root_2();
          var span = child(button);
          var span_1 = sibling(child(span), 2);
          var text = child(span_1, true);
          reset(span_1);
          reset(span);
          reset(button);
          template_effect(($0) => set_text(text, $0), [() => translate(get(step).label)]);
          delegated("click", button, function click() {
            return $$props.onStepClick?.(get(i));
          });
          append($$anchor3, button);
        };
        var consequent_1 = ($$anchor3) => {
          var span_2 = root_3();
          var span_3 = child(span_2);
          var span_4 = child(span_3);
          var text_1 = child(span_4, true);
          reset(span_4);
          reset(span_3);
          var span_5 = sibling(span_3, 2);
          var text_2 = child(span_5, true);
          reset(span_5);
          reset(span_2);
          template_effect(
            ($0, $1) => {
              set_text(text_1, $0);
              set_text(text_2, $1);
            },
            [
              () => String(get(i) + 1).padStart(2, "0"),
              () => translate(get(step).label)
            ]
          );
          append($$anchor3, span_2);
        };
        var consequent_2 = ($$anchor3) => {
          var span_6 = root_4();
          var span_7 = child(span_6);
          var span_8 = child(span_7);
          var span_9 = child(span_8);
          var text_3 = child(span_9, true);
          reset(span_9);
          reset(span_8);
          var span_10 = sibling(span_8, 2);
          var text_4 = child(span_10, true);
          reset(span_10);
          reset(span_7);
          reset(span_6);
          template_effect(
            ($0, $1) => {
              set_text(text_3, $0);
              set_text(text_4, $1);
            },
            [
              () => String(get(i) + 1).padStart(2, "0"),
              () => translate(get(step).label)
            ]
          );
          append($$anchor3, span_6);
        };
        add_svelte_meta(
          () => if_block(node, ($$render) => {
            if (get(isCompleted)) $$render(consequent);
            else if (get(isCurrent)) $$render(consequent_1, 1);
            else if (get(isUpcoming)) $$render(consequent_2, 2);
          }),
          "if",
          ProgressBar,
          24,
          4
        );
      }
      var node_1 = sibling(node, 2);
      {
        var consequent_3 = ($$anchor3) => {
          var div = root_5();
          append($$anchor3, div);
        };
        add_svelte_meta(
          () => if_block(node_1, ($$render) => {
            if (get(i) < $$props.steps.length - 1) $$render(consequent_3);
          }),
          "if",
          ProgressBar,
          58,
          4
        );
      }
      reset(li);
      append($$anchor2, li);
    }),
    "each",
    ProgressBar,
    19,
    2
  );
  reset(ol);
  reset(nav);
  append($$anchor, nav);
  return pop($$exports);
}
if (import.meta.hot) {
  ProgressBar = hmr(ProgressBar);
  import.meta.hot.accept((module) => {
    ProgressBar[HMR].update(module.default);
  });
}
var ProgressBar_default = ProgressBar;
delegate(["click"]);

// node_modules/formcomp/dist/components/layout/NavigationButtons.svelte
NavigationButtons[FILENAME] = "node_modules/formcomp/dist/components/layout/NavigationButtons.svelte";
var root_12 = add_locations(from_html(`<button type="button" class="rounded-md bg-white px-4 py-2 text-sm font-semibold text-gray-900 shadow-xs ring-1 ring-inset ring-gray-300 hover:bg-gray-50"> </button>`), NavigationButtons[FILENAME], [[23, 3]]);
var root_22 = add_locations(from_html(`<button type="button" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-xs hover:bg-indigo-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"> </button>`), NavigationButtons[FILENAME], [[34, 3]]);
var root2 = add_locations(from_html(`<div class="flex justify-between pt-6 border-t border-gray-200 mt-8"><div><!></div> <div><!></div></div>`), NavigationButtons[FILENAME], [[20, 0, [[21, 1], [32, 1]]]]);
function NavigationButtons($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, NavigationButtons);
  let showBack = prop($$props, "showBack", 3, true), showNext = prop($$props, "showNext", 3, true), nextLabel = prop($$props, "nextLabel", 3, "Next"), backLabel = prop($$props, "backLabel", 3, "Back");
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  var $$exports = { ...legacy_api() };
  var div = root2();
  var div_1 = child(div);
  var node = child(div_1);
  {
    var consequent = ($$anchor2) => {
      var button = root_12();
      var text = child(button, true);
      reset(button);
      template_effect(($0) => set_text(text, $0), [() => translate(backLabel())]);
      delegated("click", button, function(...$$args) {
        apply(() => $$props.onback, this, $$args, NavigationButtons, [25, 13]);
      });
      append($$anchor2, button);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (showBack()) $$render(consequent);
      }),
      "if",
      NavigationButtons,
      22,
      2
    );
  }
  reset(div_1);
  var div_2 = sibling(div_1, 2);
  var node_1 = child(div_2);
  {
    var consequent_1 = ($$anchor2) => {
      var button_1 = root_22();
      var text_1 = child(button_1, true);
      reset(button_1);
      template_effect(($0) => set_text(text_1, $0), [() => translate(nextLabel())]);
      delegated("click", button_1, function(...$$args) {
        apply(() => $$props.onnext, this, $$args, NavigationButtons, [36, 13]);
      });
      append($$anchor2, button_1);
    };
    add_svelte_meta(
      () => if_block(node_1, ($$render) => {
        if (showNext()) $$render(consequent_1);
      }),
      "if",
      NavigationButtons,
      33,
      2
    );
  }
  reset(div_2);
  reset(div);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  NavigationButtons = hmr(NavigationButtons);
  import.meta.hot.accept((module) => {
    NavigationButtons[HMR].update(module.default);
  });
}
var NavigationButtons_default = NavigationButtons;
delegate(["click"]);

// node_modules/formcomp/dist/components/layout/StepContainer.svelte
StepContainer[FILENAME] = "node_modules/formcomp/dist/components/layout/StepContainer.svelte";
var root_23 = add_locations(from_html(`<h2 class="text-xl font-bold text-gray-900"> </h2>`), StepContainer[FILENAME], [[22, 4]]);
var root_32 = add_locations(from_html(`<p class="mt-2 text-sm text-gray-600"> </p>`), StepContainer[FILENAME], [[25, 4]]);
var root_13 = add_locations(from_html(`<div class="mb-6"><!> <!></div>`), StepContainer[FILENAME], [[20, 2]]);
var root3 = add_locations(from_html(`<div class="space-y-8"><!> <!></div>`), StepContainer[FILENAME], [[18, 0]]);
function StepContainer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, StepContainer);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  var $$exports = { ...legacy_api() };
  var div = root3();
  var node = child(div);
  {
    var consequent_2 = ($$anchor2) => {
      var div_1 = root_13();
      var node_1 = child(div_1);
      {
        var consequent = ($$anchor3) => {
          var h2 = root_23();
          var text = child(h2, true);
          reset(h2);
          template_effect(($0) => set_text(text, $0), [() => translate($$props.title)]);
          append($$anchor3, h2);
        };
        add_svelte_meta(
          () => if_block(node_1, ($$render) => {
            if ($$props.title) $$render(consequent);
          }),
          "if",
          StepContainer,
          21,
          3
        );
      }
      var node_2 = sibling(node_1, 2);
      {
        var consequent_1 = ($$anchor3) => {
          var p = root_32();
          var text_1 = child(p, true);
          reset(p);
          template_effect(($0) => set_text(text_1, $0), [() => translate($$props.intro)]);
          append($$anchor3, p);
        };
        add_svelte_meta(
          () => if_block(node_2, ($$render) => {
            if ($$props.intro) $$render(consequent_1);
          }),
          "if",
          StepContainer,
          24,
          3
        );
      }
      reset(div_1);
      append($$anchor2, div_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.title || $$props.intro) $$render(consequent_2);
      }),
      "if",
      StepContainer,
      19,
      1
    );
  }
  var node_3 = sibling(node, 2);
  add_svelte_meta(() => snippet(node_3, () => $$props.children), "render", StepContainer, 29, 1);
  reset(div);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  StepContainer = hmr(StepContainer);
  import.meta.hot.accept((module) => {
    StepContainer[HMR].update(module.default);
  });
}
var StepContainer_default = StepContainer;

// node_modules/formcomp/dist/components/layout/QuestionGroupWrapper.svelte
QuestionGroupWrapper[FILENAME] = "node_modules/formcomp/dist/components/layout/QuestionGroupWrapper.svelte";
var root_14 = add_locations(from_html(`<h3 class="text-base font-semibold text-gray-900 mb-4"> </h3>`), QuestionGroupWrapper[FILENAME], [[21, 2]]);
var root4 = add_locations(from_html(`<div class="scroll-mt-8"><!> <div><!></div></div>`), QuestionGroupWrapper[FILENAME], [[19, 0, [[23, 1]]]]);
function QuestionGroupWrapper($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, QuestionGroupWrapper);
  let warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  var $$exports = { ...legacy_api() };
  var div = root4();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var h3 = root_14();
      var text = child(h3, true);
      reset(h3);
      template_effect(($0) => set_text(text, $0), [() => translate($$props.label)]);
      append($$anchor2, h3);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      QuestionGroupWrapper,
      20,
      1
    );
  }
  var div_1 = sibling(node, 2);
  var node_1 = child(div_1);
  add_svelte_meta(() => snippet(node_1, () => $$props.children), "render", QuestionGroupWrapper, 26, 2);
  reset(div_1);
  reset(div);
  template_effect(() => {
    set_attribute(div, "id", $$props.id);
    set_class(div_1, 1, `transition-all duration-200 rounded-lg ${warning() ? "ring-2 ring-red-300 bg-red-50/50 p-4" : ""}`);
  });
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  QuestionGroupWrapper = hmr(QuestionGroupWrapper);
  import.meta.hot.accept((module) => {
    QuestionGroupWrapper[HMR].update(module.default);
  });
}
var QuestionGroupWrapper_default = QuestionGroupWrapper;

// node_modules/formcomp/dist/components/inputs/RadioListGroup.svelte
RadioListGroup[FILENAME] = "node_modules/formcomp/dist/components/inputs/RadioListGroup.svelte";
var root_24 = add_locations(from_html(`<p class="text-gray-500"> </p>`), RadioListGroup[FILENAME], [[45, 6]]);
var root_15 = add_locations(from_html(`<div class="relative flex items-start"><div class="flex h-6 items-center"><input type="radio" class="relative size-4 appearance-none rounded-full border border-gray-300 bg-white before:absolute before:inset-1 before:rounded-full before:bg-white not-checked:before:hidden checked:border-indigo-600 checked:bg-indigo-600 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 forced-colors:appearance-auto forced-colors:before:hidden"/></div> <div class="ml-3 text-sm/6"><label class="font-medium text-gray-900"> </label> <!></div></div>`), RadioListGroup[FILENAME], [[30, 3, [[31, 4, [[32, 5]]], [42, 4, [[43, 5]]]]]]);
var root5 = add_locations(from_html(`<fieldset><div></div></fieldset>`), RadioListGroup[FILENAME], [[25, 0, [[26, 1]]]]);
function RadioListGroup($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RadioListGroup);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "radio"), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleChange(optionValue) {
    value(optionValue);
    $$props.onchange?.(optionValue);
  }
  var $$exports = { ...legacy_api() };
  var fieldset = root5();
  var div = child(fieldset);
  let classes;
  add_svelte_meta(
    () => each(div, 21, () => $$props.options, (option) => option.value, ($$anchor2, option) => {
      const checked = tag(user_derived(() => strict_equals(value(), get(option).value)), "checked");
      get(checked);
      const id = tag(user_derived(() => `${name()}-${get(option).value}`), "id");
      get(id);
      var div_1 = root_15();
      var div_2 = child(div_1);
      var input = child(div_2);
      remove_input_defaults(input);
      reset(div_2);
      var div_3 = sibling(div_2, 2);
      var label_1 = child(div_3);
      var text = child(label_1, true);
      reset(label_1);
      var node = sibling(label_1, 2);
      {
        var consequent = ($$anchor3) => {
          var p = root_24();
          var text_1 = child(p, true);
          reset(p);
          template_effect(
            ($0) => {
              set_attribute(p, "id", `${get(id) ?? ""}-desc`);
              set_text(text_1, $0);
            },
            [() => translate(get(option).description)]
          );
          append($$anchor3, p);
        };
        add_svelte_meta(
          () => if_block(node, ($$render) => {
            if (get(option).description) $$render(consequent);
          }),
          "if",
          RadioListGroup,
          44,
          5
        );
      }
      reset(div_3);
      reset(div_1);
      template_effect(
        ($0) => {
          set_attribute(input, "id", get(id));
          set_attribute(input, "aria-describedby", get(option).description ? `${get(id)}-desc` : void 0);
          set_attribute(input, "name", name());
          set_checked(input, get(checked));
          set_attribute(label_1, "for", get(id));
          set_text(text, $0);
        },
        [() => translate(get(option).label)]
      );
      delegated("change", input, function change() {
        return handleChange(get(option).value);
      });
      append($$anchor2, div_1);
    }),
    "each",
    RadioListGroup,
    27,
    2
  );
  reset(div);
  reset(fieldset);
  template_effect(
    ($0) => {
      set_attribute(fieldset, "aria-label", $0);
      classes = set_class(div, 1, "space-y-5", null, classes, {
        "ring-2": warning(),
        "ring-red-300": warning(),
        "rounded-lg": warning(),
        "p-4": warning()
      });
    },
    [() => $$props.label ? translate($$props.label) : void 0]
  );
  append($$anchor, fieldset);
  return pop($$exports);
}
if (import.meta.hot) {
  RadioListGroup = hmr(RadioListGroup);
  import.meta.hot.accept((module) => {
    RadioListGroup[HMR].update(module.default);
  });
}
var RadioListGroup_default = RadioListGroup;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/RadioCardGroup.svelte
RadioCardGroup[FILENAME] = "node_modules/formcomp/dist/components/inputs/RadioCardGroup.svelte";
var root_16 = add_locations(from_html(`<legend class="text-sm/6 font-semibold text-gray-900"> </legend>`), RadioCardGroup[FILENAME], [[34, 2]]);
var root_33 = add_locations(from_html(`<span class="mt-1 flex items-center text-sm text-gray-500"> </span>`), RadioCardGroup[FILENAME], [[62, 7]]);
var root_25 = add_locations(from_html(`<label><input type="radio" class="sr-only"/> <span class="flex flex-1"><span class="flex flex-col"><span class="block text-sm font-medium text-gray-900"> </span> <!></span></span> <svg viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M10 18a8 8 0 1 0 0-16 8 8 0 0 0 0 16Zm3.857-9.809a.75.75 0 0 0-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 1 0-1.06 1.061l2.5 2.5a.75.75 0 0 0 1.137-.089l4-5.5Z" clip-rule="evenodd"></path></svg> <span aria-hidden="true"></span></label>`), RadioCardGroup[FILENAME], [
  [
    45,
    3,
    [
      [50, 4],
      [58, 4, [[59, 5, [[60, 6]]]]],
      [66, 4, [[67, 5]]],
      [69, 4]
    ]
  ]
]);
var root6 = add_locations(from_html(`<fieldset><!> <div></div></fieldset>`), RadioCardGroup[FILENAME], [[32, 0, [[36, 1]]]]);
function RadioCardGroup($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, RadioCardGroup);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "radio-card"), warning = prop($$props, "warning", 3, false), columns = prop($$props, "columns", 3, 3);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleSelect(optionValue) {
    value(optionValue);
    $$props.onchange?.(optionValue);
  }
  const gridCols = {
    1: "sm:grid-cols-1",
    2: "sm:grid-cols-2",
    3: "sm:grid-cols-3"
  };
  var $$exports = { ...legacy_api() };
  var fieldset = root6();
  var node = child(fieldset);
  {
    var consequent = ($$anchor2) => {
      var legend = root_16();
      var text = child(legend, true);
      reset(legend);
      template_effect(($0) => set_text(text, $0), [() => translate($$props.label)]);
      append($$anchor2, legend);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      RadioCardGroup,
      33,
      1
    );
  }
  var div = sibling(node, 2);
  let classes;
  add_svelte_meta(
    () => each(div, 21, () => $$props.options, (option) => option.value, ($$anchor2, option) => {
      const checked = tag(user_derived(() => strict_equals(value(), get(option).value)), "checked");
      get(checked);
      var label_1 = root_25();
      var input = child(label_1);
      remove_input_defaults(input);
      var span = sibling(input, 2);
      var span_1 = child(span);
      var span_2 = child(span_1);
      var text_1 = child(span_2, true);
      reset(span_2);
      var node_1 = sibling(span_2, 2);
      {
        var consequent_1 = ($$anchor3) => {
          var span_3 = root_33();
          var text_2 = child(span_3, true);
          reset(span_3);
          template_effect(($0) => set_text(text_2, $0), [() => translate(get(option).description)]);
          append($$anchor3, span_3);
        };
        add_svelte_meta(
          () => if_block(node_1, ($$render) => {
            if (get(option).description) $$render(consequent_1);
          }),
          "if",
          RadioCardGroup,
          61,
          6
        );
      }
      reset(span_1);
      reset(span);
      var svg = sibling(span, 2);
      var span_4 = sibling(svg, 2);
      reset(label_1);
      template_effect(
        ($0, $1, $2) => {
          set_attribute(label_1, "aria-label", $0);
          set_attribute(label_1, "title", $1);
          set_class(label_1, 1, `relative flex cursor-pointer rounded-lg border bg-white p-4 shadow-xs focus-within:outline-hidden ${get(checked) ? "border-indigo-600 ring-2 ring-indigo-600" : "border-gray-300"}`);
          set_attribute(input, "name", name());
          set_value(input, get(option).value);
          set_checked(input, get(checked));
          set_text(text_1, $2);
          set_class(svg, 0, `size-5 text-indigo-600 ${get(checked) ? "" : "invisible"}`);
          set_class(span_4, 1, `pointer-events-none absolute -inset-px rounded-lg ${get(checked) ? "border-2 border-indigo-600" : "border-2 border-transparent"}`);
        },
        [
          () => translate(get(option).label),
          () => get(option).description ? translate(get(option).description) : void 0,
          () => translate(get(option).label)
        ]
      );
      delegated("change", input, function change() {
        return handleSelect(get(option).value);
      });
      append($$anchor2, label_1);
    }),
    "each",
    RadioCardGroup,
    43,
    2
  );
  reset(div);
  reset(fieldset);
  template_effect(() => classes = set_class(div, 1, `mt-4 grid grid-cols-1 gap-y-6 ${gridCols[columns()] ?? ""} sm:gap-x-4`, null, classes, {
    "ring-2": warning(),
    "ring-red-300": warning(),
    "rounded-lg": warning(),
    "p-2": warning()
  }));
  append($$anchor, fieldset);
  return pop($$exports);
}
if (import.meta.hot) {
  RadioCardGroup = hmr(RadioCardGroup);
  import.meta.hot.accept((module) => {
    RadioCardGroup[HMR].update(module.default);
  });
}
var RadioCardGroup_default = RadioCardGroup;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/CheckboxGroup.svelte
CheckboxGroup[FILENAME] = "node_modules/formcomp/dist/components/inputs/CheckboxGroup.svelte";
var root_26 = add_locations(from_html(`<p class="text-gray-500"> </p>`), CheckboxGroup[FILENAME], [[67, 6]]);
var root_17 = add_locations(from_html(`<div class="flex gap-3"><div class="flex h-6 shrink-0 items-center"><div class="group grid size-4 grid-cols-1"><input type="checkbox" class="col-start-1 row-start-1 appearance-none rounded-sm border border-gray-300 bg-white checked:border-indigo-600 checked:bg-indigo-600 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 forced-colors:appearance-auto"/> <svg class="pointer-events-none col-start-1 row-start-1 size-3.5 self-center justify-self-center stroke-white" viewBox="0 0 14 14" fill="none"><path class="opacity-0 group-has-checked:opacity-100" d="M3 8L6 11L11 3.5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg></div></div> <div class="text-sm/6"><label class="font-medium text-gray-900"> </label> <!></div></div>`), CheckboxGroup[FILENAME], [
  [
    47,
    3,
    [
      [48, 4, [[49, 5, [[50, 6], [59, 6, [[60, 7]]]]]]],
      [64, 4, [[65, 5]]]
    ]
  ]
]);
var root7 = add_locations(from_html(`<fieldset><div></div></fieldset>`), CheckboxGroup[FILENAME], [[42, 0, [[43, 1]]]]);
function CheckboxGroup($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, CheckboxGroup);
  let value = prop($$props, "value", 31, () => tag_proxy(proxy([]), "value")), name = prop($$props, "name", 3, "checkbox"), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleToggle(optionValue, exclusive) {
    let next2;
    if (exclusive) {
      next2 = value().includes(optionValue) ? [] : [optionValue];
    } else {
      const exclusiveValues = $$props.options.filter((o) => o.exclusive).map((o) => o.value);
      const current = value().filter((v) => !exclusiveValues.includes(v));
      if (current.includes(optionValue)) {
        next2 = current.filter((v) => strict_equals(v, optionValue, false));
      } else {
        next2 = [...current, optionValue];
      }
    }
    value(next2);
    $$props.onchange?.(next2);
  }
  var $$exports = { ...legacy_api() };
  var fieldset = root7();
  var div = child(fieldset);
  let classes;
  add_svelte_meta(
    () => each(div, 21, () => $$props.options, (option) => option.value, ($$anchor2, option) => {
      const checked = tag(user_derived(() => value().includes(get(option).value)), "checked");
      get(checked);
      const id = tag(user_derived(() => `${name()}-${get(option).value}`), "id");
      get(id);
      var div_1 = root_17();
      var div_2 = child(div_1);
      var div_3 = child(div_2);
      var input = child(div_3);
      remove_input_defaults(input);
      next(2);
      reset(div_3);
      reset(div_2);
      var div_4 = sibling(div_2, 2);
      var label_1 = child(div_4);
      var text = child(label_1, true);
      reset(label_1);
      var node = sibling(label_1, 2);
      {
        var consequent = ($$anchor3) => {
          var p = root_26();
          var text_1 = child(p, true);
          reset(p);
          template_effect(
            ($0) => {
              set_attribute(p, "id", `${get(id) ?? ""}-desc`);
              set_text(text_1, $0);
            },
            [() => translate(get(option).description)]
          );
          append($$anchor3, p);
        };
        add_svelte_meta(
          () => if_block(node, ($$render) => {
            if (get(option).description) $$render(consequent);
          }),
          "if",
          CheckboxGroup,
          66,
          5
        );
      }
      reset(div_4);
      reset(div_1);
      template_effect(
        ($0) => {
          set_attribute(input, "id", get(id));
          set_attribute(input, "aria-describedby", get(option).description ? `${get(id)}-desc` : void 0);
          set_attribute(input, "name", `${name() ?? ""}[]`);
          set_checked(input, get(checked));
          set_attribute(label_1, "for", get(id));
          set_text(text, $0);
        },
        [() => translate(get(option).label)]
      );
      delegated("change", input, function change() {
        return handleToggle(get(option).value, get(option).exclusive);
      });
      append($$anchor2, div_1);
    }),
    "each",
    CheckboxGroup,
    44,
    2
  );
  reset(div);
  reset(fieldset);
  template_effect(
    ($0) => {
      set_attribute(fieldset, "aria-label", $0);
      classes = set_class(div, 1, "space-y-5", null, classes, {
        "ring-2": warning(),
        "ring-red-300": warning(),
        "rounded-lg": warning(),
        "p-4": warning()
      });
    },
    [() => $$props.label ? translate($$props.label) : void 0]
  );
  append($$anchor, fieldset);
  return pop($$exports);
}
if (import.meta.hot) {
  CheckboxGroup = hmr(CheckboxGroup);
  import.meta.hot.accept((module) => {
    CheckboxGroup[HMR].update(module.default);
  });
}
var CheckboxGroup_default = CheckboxGroup;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/ScaleInput.svelte
ScaleInput[FILENAME] = "node_modules/formcomp/dist/components/inputs/ScaleInput.svelte";
var root_18 = add_locations(from_html(`<label><input type="radio" class="sr-only"/> <span> </span></label>`), ScaleInput[FILENAME], [[40, 4, [[41, 5], [49, 5]]]]);
var root_27 = add_locations(from_html(`<div class="flex justify-between text-xs text-gray-500 px-1"><span> </span> <span> </span></div>`), ScaleInput[FILENAME], [[54, 3, [[55, 4], [56, 4]]]]);
var root8 = add_locations(from_html(`<fieldset><div><div class="flex flex-wrap gap-2 justify-center"></div> <!></div></fieldset>`), ScaleInput[FILENAME], [[29, 0, [[30, 1, [[37, 2]]]]]]);
function ScaleInput($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, ScaleInput);
  let min = prop($$props, "min", 3, 1), max = prop($$props, "max", 3, 10), value = prop($$props, "value", 15), name = prop($$props, "name", 3, "scale"), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleSelect(n) {
    value(n);
    $$props.onchange?.(n);
  }
  const numbers = tag(user_derived(() => Array.from({ length: max() - min() + 1 }, (_, i) => min() + i)), "numbers");
  var $$exports = { ...legacy_api() };
  var fieldset = root8();
  var div = child(fieldset);
  let classes;
  var div_1 = child(div);
  add_svelte_meta(
    () => each(div_1, 20, () => get(numbers), (n) => n, ($$anchor2, n) => {
      const selected = tag(user_derived(() => strict_equals(value(), n)), "selected");
      get(selected);
      var label = root_18();
      var input = child(label);
      remove_input_defaults(input);
      var span = sibling(input, 2);
      var text = child(span, true);
      reset(span);
      reset(label);
      template_effect(() => {
        set_class(label, 1, `flex cursor-pointer items-center justify-center rounded-full size-10 text-sm font-semibold focus-within:outline-hidden ${get(selected) ? "bg-indigo-600 text-white ring-2 ring-indigo-600" : "ring-1 ring-inset ring-gray-300 bg-white text-gray-900 hover:bg-gray-50"}`);
        set_attribute(input, "name", name());
        set_value(input, n);
        set_checked(input, get(selected));
        set_text(text, n);
      });
      delegated("change", input, function change() {
        return handleSelect(n);
      });
      append($$anchor2, label);
    }),
    "each",
    ScaleInput,
    38,
    3
  );
  reset(div_1);
  var node = sibling(div_1, 2);
  {
    var consequent = ($$anchor2) => {
      var div_2 = root_27();
      var span_1 = child(div_2);
      var text_1 = child(span_1, true);
      reset(span_1);
      var span_2 = sibling(span_1, 2);
      var text_2 = child(span_2, true);
      reset(span_2);
      reset(div_2);
      template_effect(
        ($0, $1) => {
          set_text(text_1, $0);
          set_text(text_2, $1);
        },
        [
          () => $$props.minLabel ? translate($$props.minLabel) : "",
          () => $$props.maxLabel ? translate($$props.maxLabel) : ""
        ]
      );
      append($$anchor2, div_2);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.minLabel || $$props.maxLabel) $$render(consequent);
      }),
      "if",
      ScaleInput,
      53,
      2
    );
  }
  reset(div);
  reset(fieldset);
  template_effect(() => {
    set_attribute(fieldset, "aria-label", name());
    classes = set_class(div, 1, "flex flex-col gap-2", null, classes, {
      "ring-2": warning(),
      "ring-red-300": warning(),
      "rounded-lg": warning(),
      "p-3": warning()
    });
  });
  append($$anchor, fieldset);
  return pop($$exports);
}
if (import.meta.hot) {
  ScaleInput = hmr(ScaleInput);
  import.meta.hot.accept((module) => {
    ScaleInput[HMR].update(module.default);
  });
}
var ScaleInput_default = ScaleInput;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/TimeInput.svelte
TimeInput[FILENAME] = "node_modules/formcomp/dist/components/inputs/TimeInput.svelte";
var root_19 = add_locations(from_html(`<label class="block text-sm/6 font-medium text-gray-900"> </label>`), TimeInput[FILENAME], [[43, 2]]);
var root9 = add_locations(from_html(`<div><!> <div><input type="time" class="block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6"/></div></div>`), TimeInput[FILENAME], [[41, 0, [[45, 1, [[46, 2]]]]]]);
function TimeInput($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TimeInput);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "time"), step = prop($$props, "step", 3, 900), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function roundToStep(timeStr) {
    if (!timeStr || step() <= 0) return timeStr;
    const [h, m] = timeStr.split(":").map(Number);
    const stepMinutes = Math.floor(step() / 60);
    if (stepMinutes <= 0) return timeStr;
    const rounded = Math.round(m / stepMinutes) * stepMinutes;
    const finalM = rounded % 60;
    const extraH = Math.floor(rounded / 60);
    const finalH = (h + extraH) % 24;
    return `${String(finalH).padStart(2, "0")}:${String(finalM).padStart(2, "0")}`;
  }
  function handleChange(e) {
    const input = e.target;
    const rounded = roundToStep(input.value);
    value(rounded);
    input.value = rounded;
    $$props.onchange?.(rounded);
  }
  var $$exports = { ...legacy_api() };
  var div = root9();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var label_1 = root_19();
      var text = child(label_1, true);
      reset(label_1);
      template_effect(
        ($0) => {
          set_attribute(label_1, "for", name());
          set_text(text, $0);
        },
        [() => translate($$props.label)]
      );
      append($$anchor2, label_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      TimeInput,
      42,
      1
    );
  }
  var div_1 = sibling(node, 2);
  let classes;
  var input_1 = child(div_1);
  remove_input_defaults(input_1);
  reset(div_1);
  reset(div);
  template_effect(() => {
    classes = set_class(div_1, 1, "mt-2", null, classes, {
      "ring-2": warning(),
      "ring-red-300": warning(),
      "rounded-md": warning()
    });
    set_attribute(input_1, "name", name());
    set_attribute(input_1, "id", name());
    set_attribute(input_1, "step", step());
    set_value(input_1, value() ?? "");
    set_attribute(input_1, "placeholder", $$props.placeholder);
  });
  delegated("change", input_1, handleChange);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  TimeInput = hmr(TimeInput);
  import.meta.hot.accept((module) => {
    TimeInput[HMR].update(module.default);
  });
}
var TimeInput_default = TimeInput;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/NumberInput.svelte
NumberInput[FILENAME] = "node_modules/formcomp/dist/components/inputs/NumberInput.svelte";
var root_110 = add_locations(from_html(`<label class="block text-sm/6 font-medium text-gray-900"> </label>`), NumberInput[FILENAME], [[42, 2]]);
var root_28 = add_locations(from_html(`<div class="pointer-events-none absolute inset-y-0 right-0 flex items-center pr-3"><span class="text-gray-500 sm:text-sm"> </span></div>`), NumberInput[FILENAME], [[58, 3, [[59, 4]]]]);
var root10 = add_locations(from_html(`<div><!> <div><input type="number"/> <!></div></div>`), NumberInput[FILENAME], [[40, 0, [[44, 1, [[45, 2]]]]]]);
function NumberInput($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, NumberInput);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "number"), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleInput(e) {
    const input = e.target;
    const raw = input.value;
    if (strict_equals(raw, "")) {
      value(void 0);
      $$props.onchange?.(void 0);
      return;
    }
    let num = parseFloat(raw);
    if (isNaN(num)) return;
    if (strict_equals($$props.min, void 0, false) && num < $$props.min) num = $$props.min;
    if (strict_equals($$props.max, void 0, false) && num > $$props.max) num = $$props.max;
    value(num);
    $$props.onchange?.(num);
  }
  var $$exports = { ...legacy_api() };
  var div = root10();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var label_1 = root_110();
      var text = child(label_1, true);
      reset(label_1);
      template_effect(
        ($0) => {
          set_attribute(label_1, "for", name());
          set_text(text, $0);
        },
        [() => translate($$props.label)]
      );
      append($$anchor2, label_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      NumberInput,
      41,
      1
    );
  }
  var div_1 = sibling(node, 2);
  let classes;
  var input_1 = child(div_1);
  remove_input_defaults(input_1);
  var node_1 = sibling(input_1, 2);
  {
    var consequent_1 = ($$anchor2) => {
      var div_2 = root_28();
      var span = child(div_2);
      var text_1 = child(span, true);
      reset(span);
      reset(div_2);
      template_effect(($0) => set_text(text_1, $0), [() => translate($$props.unit)]);
      append($$anchor2, div_2);
    };
    add_svelte_meta(
      () => if_block(node_1, ($$render) => {
        if ($$props.unit) $$render(consequent_1);
      }),
      "if",
      NumberInput,
      57,
      2
    );
  }
  reset(div_1);
  reset(div);
  template_effect(() => {
    classes = set_class(div_1, 1, "mt-2 relative", null, classes, {
      "ring-2": warning(),
      "ring-red-300": warning(),
      "rounded-md": warning()
    });
    set_attribute(input_1, "name", name());
    set_attribute(input_1, "id", name());
    set_attribute(input_1, "min", $$props.min);
    set_attribute(input_1, "max", $$props.max);
    set_attribute(input_1, "step", $$props.step);
    set_value(input_1, value() ?? "");
    set_attribute(input_1, "placeholder", $$props.placeholder);
    set_class(input_1, 1, `block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6 ${$$props.unit ? "pr-12" : ""}`);
  });
  delegated("change", input_1, handleInput);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  NumberInput = hmr(NumberInput);
  import.meta.hot.accept((module) => {
    NumberInput[HMR].update(module.default);
  });
}
var NumberInput_default = NumberInput;
delegate(["change"]);

// node_modules/formcomp/dist/components/inputs/TextInput.svelte
TextInput[FILENAME] = "node_modules/formcomp/dist/components/inputs/TextInput.svelte";
var root_111 = add_locations(from_html(`<label class="block text-sm/6 font-medium text-gray-900"> </label>`), TextInput[FILENAME], [[29, 2]]);
var root11 = add_locations(from_html(`<div><!> <div><input class="block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6"/></div></div>`), TextInput[FILENAME], [[27, 0, [[31, 1, [[32, 2]]]]]]);
function TextInput($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TextInput);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "text"), type = prop($$props, "type", 3, "text"), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleInput(e) {
    const input = e.target;
    value(input.value);
    $$props.onchange?.(input.value);
  }
  var $$exports = { ...legacy_api() };
  var div = root11();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var label_1 = root_111();
      var text = child(label_1, true);
      reset(label_1);
      template_effect(
        ($0) => {
          set_attribute(label_1, "for", name());
          set_text(text, $0);
        },
        [() => translate($$props.label)]
      );
      append($$anchor2, label_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      TextInput,
      28,
      1
    );
  }
  var div_1 = sibling(node, 2);
  let classes;
  var input_1 = child(div_1);
  remove_input_defaults(input_1);
  reset(div_1);
  reset(div);
  template_effect(() => {
    classes = set_class(div_1, 1, "mt-2", null, classes, {
      "ring-2": warning(),
      "ring-red-300": warning(),
      "rounded-md": warning()
    });
    set_attribute(input_1, "type", type());
    set_attribute(input_1, "name", name());
    set_attribute(input_1, "id", name());
    set_value(input_1, value() ?? "");
    set_attribute(input_1, "placeholder", $$props.placeholder);
  });
  delegated("input", input_1, handleInput);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  TextInput = hmr(TextInput);
  import.meta.hot.accept((module) => {
    TextInput[HMR].update(module.default);
  });
}
var TextInput_default = TextInput;
delegate(["input"]);

// node_modules/formcomp/dist/components/inputs/TextArea.svelte
TextArea[FILENAME] = "node_modules/formcomp/dist/components/inputs/TextArea.svelte";
var root_112 = add_locations(from_html(`<label class="block text-sm/6 font-medium text-gray-900"> </label>`), TextArea[FILENAME], [[29, 2]]);
var root12 = add_locations(from_html(`<div><!> <div><textarea class="block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6"></textarea></div></div>`), TextArea[FILENAME], [[27, 0, [[31, 1, [[32, 2]]]]]]);
function TextArea($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, TextArea);
  let value = prop($$props, "value", 15), name = prop($$props, "name", 3, "textarea"), rows = prop($$props, "rows", 3, 4), warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  function handleInput(e) {
    const el = e.target;
    value(el.value);
    $$props.onchange?.(el.value);
  }
  var $$exports = { ...legacy_api() };
  var div = root12();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var label_1 = root_112();
      var text = child(label_1, true);
      reset(label_1);
      template_effect(
        ($0) => {
          set_attribute(label_1, "for", name());
          set_text(text, $0);
        },
        [() => translate($$props.label)]
      );
      append($$anchor2, label_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if ($$props.label) $$render(consequent);
      }),
      "if",
      TextArea,
      28,
      1
    );
  }
  var div_1 = sibling(node, 2);
  let classes;
  var textarea = child(div_1);
  remove_textarea_child(textarea);
  reset(div_1);
  reset(div);
  template_effect(() => {
    classes = set_class(div_1, 1, "mt-2", null, classes, {
      "ring-2": warning(),
      "ring-red-300": warning(),
      "rounded-md": warning()
    });
    set_attribute(textarea, "name", name());
    set_attribute(textarea, "id", name());
    set_attribute(textarea, "rows", rows());
    set_attribute(textarea, "placeholder", $$props.placeholder);
    set_value(textarea, value() ?? "");
  });
  delegated("input", textarea, handleInput);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  TextArea = hmr(TextArea);
  import.meta.hot.accept((module) => {
    TextArea[HMR].update(module.default);
  });
}
var TextArea_default = TextArea;
delegate(["input"]);

// node_modules/formcomp/dist/components/core/QuestionRenderer.svelte
QuestionRenderer[FILENAME] = "node_modules/formcomp/dist/components/core/QuestionRenderer.svelte";
function QuestionRenderer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, QuestionRenderer);
  let warning = prop($$props, "warning", 3, false);
  const state2 = getContext(FORM_STATE_KEY);
  const stepId = getContext(STEP_ID_KEY);
  function getValue() {
    return state2.getResponse(stepId, $$props.question.id);
  }
  function setValue(v) {
    state2.setResponse(stepId, $$props.question.id, v);
  }
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent_1 = ($$anchor2) => {
      var fragment_1 = comment();
      var node_1 = first_child(fragment_1);
      {
        var consequent = ($$anchor3) => {
          var fragment_2 = comment();
          var node_2 = first_child(fragment_2);
          {
            let $0 = user_derived(() => $$props.question.options ?? []);
            let $1 = user_derived(getValue);
            let $2 = user_derived(() => $$props.question.layout?.columns);
            add_svelte_meta(
              () => RadioCardGroup_default(node_2, {
                get options() {
                  return get($0);
                },
                get value() {
                  return get($1);
                },
                onchange: (v) => setValue(v),
                get name() {
                  return $$props.question.id;
                },
                get label() {
                  return $$props.question.label;
                },
                get warning() {
                  return warning();
                },
                get columns() {
                  return get($2);
                }
              }),
              "component",
              QuestionRenderer,
              38,
              2,
              { componentTag: "RadioCardGroup" }
            );
          }
          append($$anchor3, fragment_2);
        };
        var alternate = ($$anchor3) => {
          var fragment_3 = comment();
          var node_3 = first_child(fragment_3);
          {
            let $0 = user_derived(() => $$props.question.options ?? []);
            let $1 = user_derived(getValue);
            add_svelte_meta(
              () => RadioListGroup_default(node_3, {
                get options() {
                  return get($0);
                },
                get value() {
                  return get($1);
                },
                onchange: (v) => setValue(v),
                get name() {
                  return $$props.question.id;
                },
                get label() {
                  return $$props.question.label;
                },
                get warning() {
                  return warning();
                }
              }),
              "component",
              QuestionRenderer,
              48,
              2,
              { componentTag: "RadioListGroup" }
            );
          }
          append($$anchor3, fragment_3);
        };
        add_svelte_meta(
          () => if_block(node_1, ($$render) => {
            if (strict_equals($$props.question.displayVariant, "card")) $$render(consequent);
            else $$render(alternate, -1);
          }),
          "if",
          QuestionRenderer,
          37,
          1
        );
      }
      append($$anchor2, fragment_1);
    };
    var consequent_2 = ($$anchor2) => {
      var fragment_4 = comment();
      var node_4 = first_child(fragment_4);
      {
        let $0 = user_derived(() => $$props.question.options ?? []);
        let $1 = user_derived(() => getValue() ?? []);
        add_svelte_meta(
          () => CheckboxGroup_default(node_4, {
            get options() {
              return get($0);
            },
            get value() {
              return get($1);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get label() {
              return $$props.question.label;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          58,
          1,
          { componentTag: "CheckboxGroup" }
        );
      }
      append($$anchor2, fragment_4);
    };
    var consequent_3 = ($$anchor2) => {
      var fragment_5 = comment();
      var node_5 = first_child(fragment_5);
      {
        let $0 = user_derived(() => $$props.question.min ?? 1);
        let $1 = user_derived(() => $$props.question.max ?? 10);
        let $2 = user_derived(getValue);
        add_svelte_meta(
          () => ScaleInput_default(node_5, {
            get min() {
              return get($0);
            },
            get max() {
              return get($1);
            },
            get value() {
              return get($2);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get minLabel() {
              return $$props.question.minLabel;
            },
            get maxLabel() {
              return $$props.question.maxLabel;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          67,
          1,
          { componentTag: "ScaleInput" }
        );
      }
      append($$anchor2, fragment_5);
    };
    var consequent_4 = ($$anchor2) => {
      var fragment_6 = comment();
      var node_6 = first_child(fragment_6);
      {
        let $0 = user_derived(getValue);
        add_svelte_meta(
          () => TimeInput_default(node_6, {
            get value() {
              return get($0);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get label() {
              return $$props.question.label;
            },
            get step() {
              return $$props.question.step;
            },
            get placeholder() {
              return $$props.question.placeholder;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          78,
          1,
          { componentTag: "TimeInput" }
        );
      }
      append($$anchor2, fragment_6);
    };
    var consequent_5 = ($$anchor2) => {
      var fragment_7 = comment();
      var node_7 = first_child(fragment_7);
      {
        let $0 = user_derived(getValue);
        add_svelte_meta(
          () => NumberInput_default(node_7, {
            get value() {
              return get($0);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get label() {
              return $$props.question.label;
            },
            get min() {
              return $$props.question.min;
            },
            get max() {
              return $$props.question.max;
            },
            get step() {
              return $$props.question.step;
            },
            get placeholder() {
              return $$props.question.placeholder;
            },
            get unit() {
              return $$props.question.unit;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          88,
          1,
          { componentTag: "NumberInput" }
        );
      }
      append($$anchor2, fragment_7);
    };
    var consequent_6 = ($$anchor2) => {
      var fragment_8 = comment();
      var node_8 = first_child(fragment_8);
      {
        let $0 = user_derived(getValue);
        add_svelte_meta(
          () => TextInput_default(node_8, {
            get value() {
              return get($0);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get label() {
              return $$props.question.label;
            },
            get placeholder() {
              return $$props.question.placeholder;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          101,
          1,
          { componentTag: "TextInput" }
        );
      }
      append($$anchor2, fragment_8);
    };
    var consequent_7 = ($$anchor2) => {
      var fragment_9 = comment();
      var node_9 = first_child(fragment_9);
      {
        let $0 = user_derived(getValue);
        add_svelte_meta(
          () => TextArea_default(node_9, {
            get value() {
              return get($0);
            },
            onchange: (v) => setValue(v),
            get name() {
              return $$props.question.id;
            },
            get label() {
              return $$props.question.label;
            },
            get placeholder() {
              return $$props.question.placeholder;
            },
            get rows() {
              return $$props.question.rows;
            },
            get warning() {
              return warning();
            }
          }),
          "component",
          QuestionRenderer,
          110,
          1,
          { componentTag: "TextArea" }
        );
      }
      append($$anchor2, fragment_9);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (strict_equals($$props.question.type, "single-select")) $$render(consequent_1);
        else if (strict_equals($$props.question.type, "multi-select")) $$render(consequent_2, 1);
        else if (strict_equals($$props.question.type, "scale")) $$render(consequent_3, 2);
        else if (strict_equals($$props.question.type, "time-input")) $$render(consequent_4, 3);
        else if (strict_equals($$props.question.type, "number-input")) $$render(consequent_5, 4);
        else if (strict_equals($$props.question.type, "text-input")) $$render(consequent_6, 5);
        else if (strict_equals($$props.question.type, "textarea")) $$render(consequent_7, 6);
      }),
      "if",
      QuestionRenderer,
      36,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  QuestionRenderer = hmr(QuestionRenderer);
  import.meta.hot.accept((module) => {
    QuestionRenderer[HMR].update(module.default);
  });
}
var QuestionRenderer_default = QuestionRenderer;

// node_modules/formcomp/dist/components/inputs/LikertGroup.svelte
LikertGroup[FILENAME] = "node_modules/formcomp/dist/components/inputs/LikertGroup.svelte";
var root_29 = add_locations(from_html(`<div class="text-center text-xs text-gray-500 font-medium"> </div>`), LikertGroup[FILENAME], [[45, 4]]);
var root_113 = add_locations(from_html(`<div class="hidden sm:grid sm:gap-2"><div></div> <!></div>`), LikertGroup[FILENAME], [[42, 2, [[43, 3]]]]);
var root_42 = add_locations(from_html(`<label><input type="radio" class="sr-only"/> <span class="sm:hidden"> </span> <span class="hidden sm:inline">&bull;</span></label>`), LikertGroup[FILENAME], [[58, 5, [[59, 6], [67, 6], [68, 6]]]]);
var root_34 = add_locations(from_html(`<div class="sm:grid sm:gap-2 sm:items-center"><div class="text-sm text-gray-900 mb-2 sm:mb-0"> </div> <div class="flex flex-wrap gap-2 sm:contents"></div></div>`), LikertGroup[FILENAME], [[53, 2, [[54, 3], [55, 3]]]]);
var root13 = add_locations(from_html(`<div><!> <!></div>`), LikertGroup[FILENAME], [[33, 0]]);
function LikertGroup($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, LikertGroup);
  let warning = prop($$props, "warning", 3, false);
  const t = getContext(TRANSLATE_KEY);
  const state2 = getContext(FORM_STATE_KEY);
  const stepId = getContext(STEP_ID_KEY);
  const translate = (key2) => t ? t(key2) : key2;
  const scaleOptions = tag(user_derived(() => $$props.questions[0]?.options ?? []), "scaleOptions");
  function getQuestionValue(questionId) {
    return state2.getResponse(stepId, questionId);
  }
  function setQuestionValue(questionId, value) {
    state2.setResponse(stepId, questionId, value);
  }
  var $$exports = { ...legacy_api() };
  var div = root13();
  let classes;
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var div_1 = root_113();
      var node_1 = sibling(child(div_1), 2);
      add_svelte_meta(
        () => each(node_1, 17, () => get(scaleOptions), (option) => option.value, ($$anchor3, option) => {
          var div_2 = root_29();
          var text = child(div_2, true);
          reset(div_2);
          template_effect(($0) => set_text(text, $0), [() => translate(get(option).label)]);
          append($$anchor3, div_2);
        }),
        "each",
        LikertGroup,
        44,
        3
      );
      reset(div_1);
      template_effect(() => set_style(div_1, `grid-template-columns: 1fr repeat(${get(scaleOptions).length ?? ""}, minmax(0, 1fr))`));
      append($$anchor2, div_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (get(scaleOptions).length > 0) $$render(consequent);
      }),
      "if",
      LikertGroup,
      41,
      1
    );
  }
  var node_2 = sibling(node, 2);
  add_svelte_meta(
    () => each(node_2, 17, () => $$props.questions, (question) => question.id, ($$anchor2, question) => {
      const currentValue = tag(user_derived(() => getQuestionValue(get(question).id)), "currentValue");
      get(currentValue);
      var div_3 = root_34();
      var div_4 = child(div_3);
      var text_1 = child(div_4, true);
      reset(div_4);
      var div_5 = sibling(div_4, 2);
      add_svelte_meta(
        () => each(div_5, 21, () => get(scaleOptions), (option) => option.value, ($$anchor3, option) => {
          const selected = tag(user_derived(() => strict_equals(get(currentValue), get(option).value)), "selected");
          get(selected);
          var label = root_42();
          var input = child(label);
          remove_input_defaults(input);
          var span = sibling(input, 2);
          var text_2 = child(span, true);
          reset(span);
          next(2);
          reset(label);
          template_effect(
            ($0) => {
              set_class(label, 1, `flex cursor-pointer items-center justify-center rounded-md px-3 py-2 text-sm font-semibold focus-within:outline-hidden sm:flex-1 ${get(selected) ? "bg-indigo-600 text-white" : "ring-1 ring-inset ring-gray-300 bg-white text-gray-900 hover:bg-gray-50"}`);
              set_attribute(input, "name", get(question).id);
              set_value(input, get(option).value);
              set_checked(input, get(selected));
              set_text(text_2, $0);
            },
            [() => translate(get(option).label)]
          );
          delegated("change", input, function change() {
            return setQuestionValue(get(question).id, get(option).value);
          });
          append($$anchor3, label);
        }),
        "each",
        LikertGroup,
        56,
        4
      );
      reset(div_5);
      reset(div_3);
      template_effect(
        ($0) => {
          set_style(div_3, `grid-template-columns: 1fr repeat(${get(scaleOptions).length ?? ""}, minmax(0, 1fr))`);
          set_text(text_1, $0);
        },
        [() => translate(get(question).label)]
      );
      append($$anchor2, div_3);
    }),
    "each",
    LikertGroup,
    51,
    1
  );
  reset(div);
  template_effect(() => classes = set_class(div, 1, "space-y-4", null, classes, {
    "ring-2": warning(),
    "ring-red-300": warning(),
    "rounded-lg": warning(),
    "p-4": warning()
  }));
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  LikertGroup = hmr(LikertGroup);
  import.meta.hot.accept((module) => {
    LikertGroup[HMR].update(module.default);
  });
}
var LikertGroup_default = LikertGroup;
delegate(["change"]);

// node_modules/formcomp/dist/components/core/GroupRenderer.svelte
GroupRenderer[FILENAME] = "node_modules/formcomp/dist/components/core/GroupRenderer.svelte";
var root_43 = add_locations(from_html(`<div></div>`), GroupRenderer[FILENAME], [[60, 3]]);
var root_6 = add_locations(from_html(`<div></div>`), GroupRenderer[FILENAME], [[67, 3]]);
function GroupRenderer($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, GroupRenderer);
  let warningGroupId = prop($$props, "warningGroupId", 3, null);
  const state2 = getContext(FORM_STATE_KEY);
  const stepId = getContext(STEP_ID_KEY);
  const isWarning = tag(user_derived(() => strict_equals(warningGroupId(), $$props.group.id)), "isWarning");
  const visibleQuestions = tag(
    user_derived(() => $$props.group.questions.filter((q) => {
      if (!q.condition) return true;
      return evaluateCondition(q.condition, (sid, qid) => state2.getResponse(sid, qid), stepId);
    })),
    "visibleQuestions"
  );
  const groupVisible = tag(user_derived(() => !$$props.group.condition || evaluateCondition($$props.group.condition, (sid, qid) => state2.getResponse(sid, qid), stepId)), "groupVisible");
  const gridColsClass = tag(
    user_derived(() => strict_equals($$props.group.layout?.columns, 2) ? "grid grid-cols-1 sm:grid-cols-2 gap-6" : strict_equals($$props.group.layout?.columns, 3) ? "grid grid-cols-1 sm:grid-cols-3 gap-6" : ""),
    "gridColsClass"
  );
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent_2 = ($$anchor2) => {
      var fragment_1 = comment();
      var node_1 = first_child(fragment_1);
      add_svelte_meta(
        () => QuestionGroupWrapper_default(node_1, {
          get id() {
            return $$props.group.id;
          },
          get label() {
            return $$props.group.label;
          },
          get warning() {
            return get(isWarning);
          },
          children: wrap_snippet(GroupRenderer, ($$anchor3, $$slotProps) => {
            var fragment_2 = comment();
            var node_2 = first_child(fragment_2);
            {
              var consequent = ($$anchor4) => {
                var fragment_3 = comment();
                var node_3 = first_child(fragment_3);
                add_svelte_meta(
                  () => LikertGroup_default(node_3, {
                    get questions() {
                      return get(visibleQuestions);
                    },
                    get warning() {
                      return get(isWarning);
                    }
                  }),
                  "component",
                  GroupRenderer,
                  57,
                  3,
                  { componentTag: "LikertGroup" }
                );
                append($$anchor4, fragment_3);
              };
              var consequent_1 = ($$anchor4) => {
                var div = root_43();
                add_svelte_meta(
                  () => each(div, 21, () => get(visibleQuestions), (question) => question.id, ($$anchor5, question) => {
                    var fragment_4 = comment();
                    var node_4 = first_child(fragment_4);
                    add_svelte_meta(
                      () => QuestionRenderer_default(node_4, {
                        get question() {
                          return get(question);
                        }
                      }),
                      "component",
                      GroupRenderer,
                      62,
                      5,
                      { componentTag: "QuestionRenderer" }
                    );
                    append($$anchor5, fragment_4);
                  }),
                  "each",
                  GroupRenderer,
                  61,
                  4
                );
                reset(div);
                template_effect(() => set_class(div, 1, clsx(get(gridColsClass) || "space-y-6")));
                append($$anchor4, div);
              };
              var alternate = ($$anchor4) => {
                var div_1 = root_6();
                add_svelte_meta(
                  () => each(div_1, 21, () => get(visibleQuestions), (question) => question.id, ($$anchor5, question) => {
                    var fragment_5 = comment();
                    var node_5 = first_child(fragment_5);
                    add_svelte_meta(
                      () => QuestionRenderer_default(node_5, {
                        get question() {
                          return get(question);
                        },
                        get warning() {
                          return get(isWarning);
                        }
                      }),
                      "component",
                      GroupRenderer,
                      69,
                      5,
                      { componentTag: "QuestionRenderer" }
                    );
                    append($$anchor5, fragment_5);
                  }),
                  "each",
                  GroupRenderer,
                  68,
                  4
                );
                reset(div_1);
                template_effect(() => set_class(div_1, 1, clsx(get(gridColsClass) || "space-y-6")));
                append($$anchor4, div_1);
              };
              add_svelte_meta(
                () => if_block(node_2, ($$render) => {
                  if (strict_equals($$props.group.renderMode, "likert-batch")) $$render(consequent);
                  else if (strict_equals($$props.group.renderMode, "inline")) $$render(consequent_1, 1);
                  else $$render(alternate, -1);
                }),
                "if",
                GroupRenderer,
                55,
                2
              );
            }
            append($$anchor3, fragment_2);
          }),
          $$slots: { default: true }
        }),
        "component",
        GroupRenderer,
        54,
        1,
        { componentTag: "QuestionGroupWrapper" }
      );
      append($$anchor2, fragment_1);
    };
    add_svelte_meta(
      () => if_block(node, ($$render) => {
        if (get(groupVisible) && get(visibleQuestions).length > 0) $$render(consequent_2);
      }),
      "if",
      GroupRenderer,
      53,
      0
    );
  }
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  GroupRenderer = hmr(GroupRenderer);
  import.meta.hot.accept((module) => {
    GroupRenderer[HMR].update(module.default);
  });
}
var GroupRenderer_default = GroupRenderer;

// node_modules/formcomp/dist/components/core/FormStep.svelte
FormStep[FILENAME] = "node_modules/formcomp/dist/components/core/FormStep.svelte";
function FormStep($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, FormStep);
  let warningGroupId = prop($$props, "warningGroupId", 3, null);
  setContext(STEP_ID_KEY, $$props.stepConfig.id);
  var $$exports = { ...legacy_api() };
  var fragment = comment();
  var node = first_child(fragment);
  add_svelte_meta(
    () => StepContainer_default(node, {
      get title() {
        return $$props.stepConfig.label;
      },
      get intro() {
        return $$props.stepConfig.intro;
      },
      children: wrap_snippet(FormStep, ($$anchor2, $$slotProps) => {
        var fragment_1 = comment();
        var node_1 = first_child(fragment_1);
        add_svelte_meta(
          () => each(node_1, 17, () => $$props.stepConfig.groups, (group) => group.id, ($$anchor3, group) => {
            var fragment_2 = comment();
            var node_2 = first_child(fragment_2);
            add_svelte_meta(
              () => GroupRenderer_default(node_2, {
                get group() {
                  return get(group);
                },
                get warningGroupId() {
                  return warningGroupId();
                }
              }),
              "component",
              FormStep,
              20,
              2,
              { componentTag: "GroupRenderer" }
            );
            append($$anchor3, fragment_2);
          }),
          "each",
          FormStep,
          19,
          1
        );
        append($$anchor2, fragment_1);
      }),
      $$slots: { default: true }
    }),
    "component",
    FormStep,
    18,
    0,
    { componentTag: "StepContainer" }
  );
  append($$anchor, fragment);
  return pop($$exports);
}
if (import.meta.hot) {
  FormStep = hmr(FormStep);
  import.meta.hot.accept((module) => {
    FormStep[HMR].update(module.default);
  });
}
var FormStep_default = FormStep;

// node_modules/formcomp/dist/components/core/MultiStepForm.svelte
MultiStepForm[FILENAME] = "node_modules/formcomp/dist/components/core/MultiStepForm.svelte";
var root14 = add_locations(from_html(`<div class="mx-auto max-w-4xl"><!> <!> <!></div>`), MultiStepForm[FILENAME], [[93, 0]]);
function MultiStepForm($$anchor, $$props) {
  check_target(new.target);
  push($$props, true, MultiStepForm);
  const formState = $$props.state ?? createFormState($$props.config);
  let warningGroupId = tag(state(null), "warningGroupId");
  setContext(FORM_STATE_KEY, formState);
  if ($$props.translate) {
    setContext(TRANSLATE_KEY, $$props.translate);
  }
  const currentStep = tag(user_derived(() => $$props.config.steps[formState.currentStepIndex]), "currentStep");
  const isFirstStep = tag(user_derived(() => strict_equals(formState.currentStepIndex, 0)), "isFirstStep");
  const isLastStep = tag(user_derived(() => strict_equals(formState.currentStepIndex, $$props.config.steps.length - 1)), "isLastStep");
  function handleNext() {
    if (!get(currentStep)) return;
    const result = validateStep(get(currentStep), (sid, qid) => formState.getResponse(sid, qid), get(currentStep).id);
    if (!result.isValid) {
      set(warningGroupId, result.firstIncompleteGroupId, true);
      if (get(warningGroupId)) {
        const el = document.getElementById(get(warningGroupId));
        el?.scrollIntoView({ behavior: "smooth", block: "center" });
      }
      return;
    }
    set(warningGroupId, null);
    const fromIndex = formState.currentStepIndex;
    if (get(isLastStep)) {
      $$props.callbacks?.onFormComplete?.(Object.fromEntries($$props.config.steps.map((s) => [s.id, formState.getStepResponses(s.id)])));
    } else {
      $$props.callbacks?.onStepComplete?.(get(currentStep).id, fromIndex);
      formState.nextStep();
      $$props.callbacks?.onStepChange?.(fromIndex, formState.currentStepIndex);
      window.scrollTo({ top: 0, behavior: "smooth" });
    }
  }
  function handleBack() {
    set(warningGroupId, null);
    const fromIndex = formState.currentStepIndex;
    formState.prevStep();
    $$props.callbacks?.onStepChange?.(fromIndex, formState.currentStepIndex);
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
  function handleStepClick(index) {
    if (index < formState.currentStepIndex) {
      set(warningGroupId, null);
      const fromIndex = formState.currentStepIndex;
      formState.goToStep(index);
      $$props.callbacks?.onStepChange?.(fromIndex, index);
      window.scrollTo({ top: 0, behavior: "smooth" });
    }
  }
  var $$exports = { ...legacy_api() };
  var div = root14();
  var node = child(div);
  add_svelte_meta(
    () => ProgressBar_default(node, {
      get steps() {
        return $$props.config.steps;
      },
      get currentIndex() {
        return formState.currentStepIndex;
      },
      onStepClick: handleStepClick
    }),
    "component",
    MultiStepForm,
    94,
    1,
    { componentTag: "ProgressBar" }
  );
  var node_1 = sibling(node, 2);
  {
    var consequent = ($$anchor2) => {
      var fragment = comment();
      var node_2 = first_child(fragment);
      add_svelte_meta(
        () => key(node_2, () => get(currentStep).id, ($$anchor3) => {
          var fragment_1 = comment();
          var node_3 = first_child(fragment_1);
          add_svelte_meta(
            () => FormStep_default(node_3, {
              get stepConfig() {
                return get(currentStep);
              },
              get warningGroupId() {
                return get(warningGroupId);
              }
            }),
            "component",
            MultiStepForm,
            102,
            3,
            { componentTag: "FormStep" }
          );
          append($$anchor3, fragment_1);
        }),
        "key",
        MultiStepForm,
        101,
        2
      );
      append($$anchor2, fragment);
    };
    add_svelte_meta(
      () => if_block(node_1, ($$render) => {
        if (get(currentStep)) $$render(consequent);
      }),
      "if",
      MultiStepForm,
      100,
      1
    );
  }
  var node_4 = sibling(node_1, 2);
  {
    let $0 = user_derived(() => !get(isFirstStep));
    let $1 = user_derived(() => get(isLastStep) ? "Submit" : "Next");
    add_svelte_meta(
      () => NavigationButtons_default(node_4, {
        get showBack() {
          return get($0);
        },
        showNext: true,
        get nextLabel() {
          return get($1);
        },
        onback: handleBack,
        onnext: handleNext
      }),
      "component",
      MultiStepForm,
      106,
      1,
      { componentTag: "NavigationButtons" }
    );
  }
  reset(div);
  append($$anchor, div);
  return pop($$exports);
}
if (import.meta.hot) {
  MultiStepForm = hmr(MultiStepForm);
  import.meta.hot.accept((module) => {
    MultiStepForm[HMR].update(module.default);
  });
}
var MultiStepForm_default = MultiStepForm;
export {
  CheckboxGroup_default as CheckboxGroup,
  FORM_STATE_KEY,
  FormStep_default as FormStep,
  GroupRenderer_default as GroupRenderer,
  LikertGroup_default as LikertGroup,
  MultiStepForm_default as MultiStepForm,
  NavigationButtons_default as NavigationButtons,
  NumberInput_default as NumberInput,
  ProgressBar_default as ProgressBar,
  QuestionGroupWrapper_default as QuestionGroupWrapper,
  QuestionRenderer_default as QuestionRenderer,
  RadioCardGroup_default as RadioCardGroup,
  RadioListGroup_default as RadioListGroup,
  STEP_ID_KEY,
  ScaleInput_default as ScaleInput,
  StepContainer_default as StepContainer,
  TRANSLATE_KEY,
  TextArea_default as TextArea,
  TextInput_default as TextInput,
  TimeInput_default as TimeInput,
  createFormState,
  evaluateCondition,
  validateStep
};
//# sourceMappingURL=formcomp.js.map
