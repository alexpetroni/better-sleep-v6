import type { Condition } from '../types.js';
export declare function evaluateCondition(condition: Condition, getResponse: (stepId: string, questionId: string) => unknown, currentStepId: string): boolean;
