import type { StepConfig } from '../types.js';
export interface ValidationResult {
    isValid: boolean;
    firstIncompleteGroupId: string | null;
}
export declare function validateStep(stepConfig: StepConfig, getResponse: (stepId: string, questionId: string) => unknown, currentStepId: string): ValidationResult;
